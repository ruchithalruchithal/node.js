/*find the cube of a number?
1) understannt the problem - i have a 1 argument (a),
 i need to implement the function cubeNum(a),
use multiplication operator to find the cube of (a),
creat a variable and store the valuse in result.

2) input and output - 
input: 2
output:8*/

function cubeNum(a){
    let result = (a*a*a);
    return result;
}
let returnResult = cubeNum(2);
console.log(returnResult);


